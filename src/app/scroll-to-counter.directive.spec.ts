import { ScrollToCounterDirective } from './scroll-counter.directive';

describe('ScrollToCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToCounterDirective();
    expect(directive).toBeTruthy();
  });
});
